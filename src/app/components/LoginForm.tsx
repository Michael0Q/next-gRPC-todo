'use client'
const LoginForm = () =>{
    return(
        <div className="loginform-container">
            <input type="text" />
            <input type="password" />
            <button onSubmit={() => {}}></button>
        </div>
    )
}

export default LoginForm